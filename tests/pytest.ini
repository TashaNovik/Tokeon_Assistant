[pytest]
markers =
    unit: Unit tests (run these by default)
    integration: Integration tests that test multiple components together
    slow: Tests that take more than average time to run
testpaths = tests
python_classes = Test*
python_files = test_*.py
python_functions = test_*
addopts = -v 